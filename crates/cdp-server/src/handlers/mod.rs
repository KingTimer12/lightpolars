//! One module per group of CDP methods. Each exposes a single `handle` that
//! the session's routing table dispatches to.

pub mod page;
pub mod print;
pub mod runtime;
pub mod screenshot;
pub mod target;
